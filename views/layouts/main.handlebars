<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Roam Ready</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css"
    />
  </head>
  <body>
    <header>
      <div class="column is-full py-5">
        <h1 class="is-size-1 has-text-centered has-text-black">Roam Ready</h1>
      </div>
      <nav class="">
        <a href="/" class="mx-4 has-text-black">Home</a>
        <a href="<!--Add Itineraries link-->" class="mx-4 has-text-black"
          >Public Itineraries</a
        >
        {{#if logged_in}}
        <a href="/dashboard" class="has-text-black">My Dashboard</a>
        <a id="logout" class="has-text-black">Logout</a>
        {{else}}
        <a href="/login" class=" mx-4 has-text-black">Login</a>
        {{/if}}
      </nav>
    </header>
    {{{ body }}}
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
  </body>
</html>
