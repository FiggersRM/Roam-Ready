<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Roam Ready</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css"
    />
  </head>
  <body>
    <header>
      <div class="column is-full" id="hdrDiv">
        <h1 class="is-size-1 has-text-centered has-text-black"><img src="/assets/images/Roam_Ready-transformed.png" id="hdrImg"></h1>
      </div>
      <nav class="navbar py-4" id="is-nav">
        <a href="/" class="mx-4 has-text-black is-underlined is-hoverable navbarItem">Home</a>
        {{!-- <a href="<!--Add Itineraries link-->" class="mx-4 has-text-black"
          >Public Itineraries</a --}}
        
        {{#if logged_in}}
        <a href="/dashboard" class="has-text-black mx-4 is-underlined">My Dashboard</a>
        <a id="logout" class="has-text-black mx-4 is-underlined">Logout</a>
        {{else}}
        <a href="/login" class=" mx-4 has-text-black is-underlined">Login</a>
        {{/if}}
      </nav>
    </header>
    {{{ body }}}
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
  </body>
</html>
